<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="pt.ulisboa.tecnico.sise.seproject.insure">

    <uses-permission android:name="android.permission.INTERNET"></uses-permission>
    <application
        android:name="pt.ulisboa.tecnico.sise.seproject.insure.GlobalState"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">
        <activity android:name=".activities.MyClaimsActivity"></activity>
        <activity android:name=".activities.ReadClaimActivity"></activity>
        <activity android:name=".activities.MyInformationActivity"></activity>
        <activity android:name=".activities.MainPageActivity"></activity>
        <activity android:name=".activities.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activities.NewClaimActivity"></activity>
    </application>

</manifest>